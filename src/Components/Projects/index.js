import "./index.css";

const Projects = (props) => {
  const { projectList } = props;

  console.log(projectList);

  return (
    <div>
      <h1>Projects</h1>
    </div>
  );
};

export default Projects;
